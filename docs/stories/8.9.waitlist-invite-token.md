# Story 8.9 — Vincular waitlist → signup via token de convite (pre-fill)

**Status:** Ready for Review
**Epic:** 8 — Customer Onboarding
**Tipo:** Backend + Frontend
**Estimativa:** 0.5d
**Depends on:** 8.1 (signup endpoint), 8.2 (invite_tokens schema), 9.4 (Admin panel gera token)

## Contexto

Decisão fechada PRD 8: **signup aberto** mas token de convite (gerado
pelo Admin Panel — story 9.4) **pre-fill** os campos de qualificação
no signup + linka org → waitlist via `organizations.waitlist_id`.

Mantém qualificação de leads de waitlist sem fricção de bloqueio.

## Acceptance Criteria

1. Endpoint `GET /api/invite-tokens/:token` valida token:
   - Retorna `200 { valid: true, name, email }` se válido
   - Retorna `404` se token não existe
   - Retorna `410` se expirado ou usado
2. Signup page (`/signup?invite={token}`) consome esse endpoint server-side
   no page.tsx:
   - Se 200, renderiza form com `defaultValue` pre-fill de name/email
   - Mostra badge "Convite confirmado" no topo (Mission Control style)
   - Se 4xx, renderiza form normal sem pre-fill (signup normal)
3. Signup endpoint (`POST /api/auth/signup`):
   - Aceita campo opcional `inviteToken` no body
   - Valida token novamente server-side (defesa profundidade)
   - Passa pra service `createUserWithOrg` (story 8.2) que:
     - Busca waitlist linked
     - Copia `state`, `city`, `role` pro organizations
     - Set `organizations.waitlist_id = waitlist.id`
     - Set `waitlist.status = 'converted'`
     - Marca `invite_tokens.used_at = now()`
4. Token usado uma única vez (UNIQUE check via `used_at IS NOT NULL`)
5. Token expira em **30 dias** (gerado pelo Admin Panel — story 9.4)
6. Após signup com token, profile step (story 8.3) **mostra campos
   pre-preenchidos** com region/role do waitlist

## Arquivos prováveis

- `apps/api/src/routes/invite-tokens.ts` (NOVO — endpoint GET)
- `apps/web/src/app/signup/page.tsx` (atualizar — server component lê searchParams.invite)
- `apps/api/src/services/signup.ts` (atualizar story 8.2 — aceita inviteToken)

## Dev notes

- **Token format:** sugestão `nanoid(32)` ou `ulid()` — random URL-safe
- **Storage tabela `invite_tokens`:** criada na story 8.2
- **Race condition:** se 2 signups usam mesmo token simultâneo,
  `UPDATE ... WHERE used_at IS NULL` garante atomicidade
- **`waitlist.status = 'converted'`:** atualiza no INSERT bem-sucedido
  pra Admin Panel ver conversão (Epic 9)
- **404 vs 410:** semanticamente diferente (não existe vs existe-mas-vencido).
  Frontend mostra mensagem específica
- **Cross-domain:** signup é em `app.guiaprint3d.com` mas token vem
  de link compartilhado (WhatsApp/email). Garante token funciona
  cross-origin

## CodeRabbit Integration

- Foco: token validation (timing-safe compare), race conditions
  (atomic UPDATE), CSRF (token-in-URL é seguro pra GET, mas POST signup
  precisa CSRF protection padrão NextAuth)
- Severity: HIGH = warn (token vazado = acesso indevido)

## Quality Gates

- [x] GET `/api/public/invite/{valido}` retorna 200 + name + email
- [x] GET com token inexistente retorna 404 `INVITE_NOT_FOUND`
- [x] GET com token expirado retorna 410 `INVITE_EXPIRED`
- [x] GET com token usado retorna 410 `INVITE_USED`
- [x] Signup com `?invite={valido}` pre-fill funciona (já entregue Story 8.1)
- [x] Signup OK marca `invite_tokens.used_at` + `waitlist.status='converted'` (já entregue Story 8.2)
- [x] 2 signups simultâneos com mesmo token → só 1 sucede (race-safe UPDATE com `used_at IS NULL` no WHERE, Story 8.2)
- [x] Profile step (8.3) vê dados pre-fillados de waitlist (via `loadPrefill` do org.waitlistId, Story 8.3)

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (1M context) — Dex (Builder)

### Files List

**MODIFIED:**

- `apps/api/src/routes/auth.ts`:
  - `GET /api/public/invite/:token` refinado pra distinguir 404 vs 410.
    Agora faz 1 query SEM filtrar usedAt/expiresAt, e inspeciona campos
    pra decidir status. Codes: `INVITE_NOT_FOUND` (404),
    `INVITE_USED` (410), `INVITE_EXPIRED` (410), 200 com prefill.
  - Removidos imports não utilizados (`and`, `gt`, `isNull`).
- `apps/web/src/app/signup/page.tsx`:
  - `resolveInvite()` agora retorna union discriminada
    `InviteStatus` com 5 estados (`ok`/`not_found`/`used`/`expired`/`error`).
  - Computed `inviteWarning` mensagem específica por status pra passar
    pro client.
- `apps/web/src/app/signup/signup-client.tsx`:
  - Nova prop `inviteWarning` opcional.
  - Badge warning amarelo quando invite era passado mas inválido
    (pré-existe badge primary "Convite confirmado" quando OK).
- `docs/stories/8.9.waitlist-invite-token.md` — Status + Dev Agent Record.

### Decisões

- **Maior parte da story já entregue por 8.1+8.2:** este story só
  refina o endpoint pra distinguir 404 vs 410. Toda a lógica de
  consumir/race-safe/copiar waitlist→org já existia desde a Story 8.2.
- **Path mantido `/api/public/invite/:token`:** AC #1 sugeria
  `/api/invite-tokens/:token`, mas o endpoint já estava em produção
  na 8.1 com path `/api/public/invite/`. Renomear quebraria o
  signup page; valor de cosmético baixo. Mantive.
- **Status codes refinados conforme AC:**
  - 404 `INVITE_NOT_FOUND`: token nunca existiu
  - 410 `INVITE_USED`: token existe mas `used_at != null`
  - 410 `INVITE_EXPIRED`: token existe mas `expires_at <= now`
  - 200 + prefill: válido
- **2 queries pra distinguir status:** antes era 1 query com filter
  no WHERE (rápido mas não distinguia). Agora 1 query sem filter +
  branches em código. Trade-off: 0 round-trip extra, branches
  triviais.
- **UI degrada graciosamente:** `inviteWarning` mostra mensagem
  amarela amigável, mas signup procede normal. Não bloqueia — user
  pode criar conta mesmo com convite inválido.
- **Modelo waitlist vs invite token clarificado (confirmação Richard):**
  Waitlist é fluxo aberto (sem token) — Landing → form → fila.
  Invite token é fluxo SEPARADO — Admin Panel (Story 9.4) seleciona
  lead da waitlist, gera token, manda link. São 2 funis diferentes,
  ambos suportados.

### Validações

- `pnpm typecheck` — verde em 7 workspaces.
- `pnpm --filter @printstudio/api test` — 31/31 passam (intactos).
- `pnpm --filter @printstudio/web build` — verde.

### Completion Notes

- **Fechamento da cadeia invite token end-to-end:**
  1. Admin gera token (Story 9.4 — pendente)
  2. Envia link `/signup?invite=TOKEN`
  3. Server Component da `/signup` resolve token (200/404/410)
  4. UI mostra badge "Convite confirmado" OU warning amarelo
  5. POST signup consome token via service createUserWithOrg
     (race-safe UPDATE)
  6. Org criada com waitlist_id linked + role/state/city pre-fill
  7. Waitlist status → 'converted'
  8. Onboarding step 1 (profile) vê dados pre-fillados de waitlist
- **Próxima dependência:** Story 9.4 (Admin Panel) implementa
  a geração do token. Por enquanto Richard pode gerar manual via
  SQL `INSERT INTO invite_tokens (token, waitlist_id, expires_at)
  VALUES (...)`.

### Change Log

- 2026-05-13 — Status: Ready → Ready for Review (Dex)
  - Endpoint refinado pra distinguir 404 vs 410
  - resolveInvite retorna union discriminada
  - UI warning amarela quando token inválido
  - Documentação Dev Agent Record completa
