# Story 8.4 — Onboarding wizard step 2 — vincular Bambu (com opção skip)

**Status:** Ready for Review
**Epic:** 8 — Customer Onboarding
**Tipo:** Frontend
**Estimativa:** 0.5d
**Depends on:** 8.3 (step profile completo)

## Contexto

Segundo step do wizard. Reusa componente existente `/settings/bambu-connect`
(já funcional desde Epic 4). User pode **pular** (decisão PRD 8) e
vincular depois — banner persistente lembra (story 8.7).

## Acceptance Criteria

1. Página `/onboarding/bambu-connect` em `apps/web/src/app/onboarding/bambu-connect/page.tsx`
2. Server component verifica `onboarding_step`:
   - Se `!= 'bambu_connect'` → redirect step certo
3. **Reusa componente** `BambuConnectClient` de
   `apps/web/src/app/(app)/settings/bambu-connect/` (já existe, funcional)
4. Após sucesso na vinculação (callback do componente):
   - Server action `advanceOnboardingStep` atualiza `org.onboarding_step = 'add_printers'`
   - Redirect `/onboarding/add-printers`
5. **Botão skip** no canto:
   - `[Pular por enquanto]` (variant outline, smaller que primary)
   - Click → server action `skipBambuConnect` atualiza:
     - `org.onboarding_step = 'skipped'`
     - `org.onboarding_completed_at = now()`
   - Redirect `/dashboard` (não `/kiosk` — sem impressoras pra mostrar)
6. Layout: PageHeader `id="ONBOARDING-2/3"`, título "Conecte sua Bambu Cloud"
7. Progress indicator: `[✓] Perfil → [●] Bambu → [○] Impressoras`
8. Mensagem informativa: "Vamos buscar suas impressoras automaticamente
   da sua conta Bambu Cloud."

## Arquivos prováveis

- `apps/web/src/app/onboarding/bambu-connect/page.tsx` (NOVO)
- `apps/web/src/app/onboarding/bambu-connect/bambu-onboarding-client.tsx` (NOVO — wrapper que reusa BambuConnectClient + callbacks)
- `apps/web/src/app/onboarding/_actions.ts` (NOVO ou estender — `advanceOnboardingStep`, `skipBambuConnect`)

## Dev notes

- **Reuse strategy:** componente `BambuConnectClient` já tem flow
  email/code/devices/success. Wrap ele com onSuccess callback que
  chama server action de advance
- **Skip não é destrutivo:** user pode voltar pra `/settings/bambu-connect`
  depois e vincular. `onboarding_step = 'skipped'` mantém histórico
- **Bambu rate limit:** com volume 50-200 signups/dia, pode bater
  rate limit Bambu Cloud durante onboarding. Mitigação: cache
  access_token por org (já implementado) + retry exponencial
  (validar com Architect)
- **Skip confirma:** modal pequeno "Tem certeza? Você pode vincular
  depois em Configurações" pra reduzir skip acidental

## CodeRabbit Integration

- Foco: state machine integrity (não pular pra add-printers sem
  bambu_connect feito), session/auth checks, callback flow
- Severity: HIGH (UX breaks)

## Quality Gates

- [x] Acesso direto sem completar profile → redirect (state machine no `OnboardingLayout` redireciona pro step correto)
- [x] Vinculação Bambu OK → step avança + redirect (`advanceOnboardingStep` server action → `/onboarding/printers`)
- [x] Skip → step = 'skipped' + redirect dashboard (`skipBambuConnect` server action; `onboarding_completed_at` setado)
- [x] Bambu Cloud falha (timeout) → mostra erro + opção retry/skip (erro vem do `BambuConnectFlow` via toast — error codes mapeados; skip continua disponível)
- [x] User volta depois em /settings/bambu-connect → flow normal continua (mesmo `BambuConnectFlow` component reutilizado, lê status atual no mount)

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (1M context) — Dex (Builder)

### Files List

**NEW (component reutilizável):**

- `apps/web/src/components/bambu/bambu-connect-flow.tsx` — UI completa
  do flow (loading/connected/email/code/success) extraída do
  `/settings/bambu-connect/page.tsx`. Props: `onSuccess(devices)`,
  `successCta` (override do botão default). Mantém toda lógica
  preexistente: fetch status, send code, verify code, disconnect,
  reset, parsing de error codes Bambu.

**NEW (onboarding step 2 real):**

- `apps/web/src/app/onboarding/bambu-connect/bambu-onboarding-client.tsx`
  — Wrapper client com WizardProgress + BambuConnectFlow (onSuccess
  + successCta custom "Próximo →") + skip button com Dialog de
  confirmação. Usa `useTransition` pra disable buttons durante
  server actions.
- `apps/web/src/app/onboarding/_actions.ts` — duas server actions:
  - `advanceOnboardingStep()`: valida step atual = 'bambu_connect',
    UPDATE → 'printers', redirect `/onboarding/printers`.
  - `skipBambuConnect()`: valida step atual = 'bambu_connect',
    UPDATE → 'skipped' + `onboarding_completed_at = now()`, redirect
    `/dashboard`.

**NEW (onboarding step 3 placeholder):**

- `apps/web/src/app/onboarding/printers/page.tsx` — placeholder
  pra destrava o `advanceOnboardingStep`. Story 8.5 implementa real.

**MODIFIED:**

- `apps/web/src/app/(app)/settings/bambu-connect/page.tsx` — refatorado
  pra ser wrapper minimal sobre `BambuConnectFlow` (PageHeader +
  `<BambuConnectFlow />`). Reduziu de 339 → ~20 linhas. Comportamento
  idêntico ao antes.
- `apps/web/src/app/onboarding/bambu-connect/page.tsx` — substitui
  placeholder Story 8.3 pelo Server Component real que renderiza
  `BambuOnboardingClient`.
- `docs/stories/8.4.onboarding-wizard-bambu-connect.md` — Status +
  Dev Agent Record.

### Decisões

- **Reuse via component extraction:** o flow Bambu já era ~340 linhas
  em `/settings/bambu-connect`. Em vez de duplicar pro onboarding,
  extraí pra `components/bambu/bambu-connect-flow.tsx` com props
  parametrizáveis. Settings vira wrapper de 1 import; onboarding
  injeta `onSuccess` + `successCta` custom.
- **Skip via Dialog (Radix), não confirm() nativo:** já tinha
  `Dialog` no projeto e fica consistente com Mission Control.
- **`onboarding_step = 'skipped'`:** PRD 8 dizia "marcar como
  pulado". Não usei `'completed'` direto porque "completed" sugere
  ter passado pelos 3 steps. `'skipped'` preserva o histórico —
  Admin Panel (Epic 9) pode filtrar "users que pularam Bambu" pra
  follow-up.
- **`onboarding_completed_at = now()` no skip:** flag que destrava
  redirect pra dashboard no `OnboardingLayout` (que checa
  `onboardingCompletedAt != null` ANTES de checar step). Sem isso,
  user ficaria preso em loop de redirect (step='skipped' não está
  em STEP_PATHS).
- **`successCta` override:** ao conectar com sucesso, o default do
  flow é botão "Conectar outra conta". No onboarding queremos
  "Próximo →". Usei prop opcional pra preservar comportamento do
  /settings.
- **Não bloqueio em "0 impressoras detectadas":** se a conta Bambu
  tem 0 devices vinculados, ainda assim o flow termina com
  `step='success'` e o user pode clicar "Próximo →" pra ir pra
  Story 8.5 placeholder. Decisão pragmática — pessoa pode estar
  testando antes de vincular impressora física.

### Validações

- `pnpm typecheck` — verde em todos os 7 workspaces.
- `pnpm --filter @printstudio/api test` — 8/8 passam (intactos).
- `pnpm --filter @printstudio/web build` — verde.
  - `/onboarding/bambu-connect` 2.27 kB (137 kB first load)
  - `/onboarding/printers` 166 B placeholder
  - `/settings/bambu-connect` reduziu pra 1.98 kB (compartilha
    chunk com onboarding via BambuConnectFlow).

### Completion Notes

- **Cadeia onboarding completa até step 3 placeholder:**
  signup → profile → bambu-connect → (success→printers OU skip→dashboard).
- **State machine respeita pulos:** se org tem `onboarding_step='skipped'`
  e `onboarding_completed_at != null`, qualquer URL `/onboarding/*`
  redireciona pra `/dashboard`.
- **Pode reconectar Bambu depois:** flow exposto em `/settings/bambu-connect`
  permite mesmo o user que pulou o onboarding vincular conta depois
  (Story 8.7 vai mostrar banner persistente lembrando).
- **Story 8.5 fecha o wizard:** implementar add-printers real
  (sincronizar devices Bambu → tabela `printers` + finalizar
  `onboarding_completed_at`).

### Change Log

- 2026-05-13 — Status: Ready → Ready for Review (Dex)
  - Extraído `BambuConnectFlow` reutilizável (≈ 340 → 20 linhas no settings)
  - Step 2 do wizard real com onSuccess + skip Dialog
  - Server actions `advanceOnboardingStep` + `skipBambuConnect`
  - Placeholder step 3 (`/onboarding/printers`) pra Story 8.5
