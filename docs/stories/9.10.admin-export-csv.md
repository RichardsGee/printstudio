# Story 9.10 — Export CSV (waitlist, users, subscriptions)

**Status:** Ready for Review (V1 — subscriptions/audit export pendem)
**Epic:** 9 — Internal Admin Panel
**Tipo:** Backend + Frontend
**Estimativa:** 0.5d
**Depends on:** 9.3, 9.5, 9.7 (entidades existem)

## Contexto

Permite Richard exportar dados pra trabalhar offline (planilha,
análise externa) e compliance LGPD (portabilidade de dados).

## Acceptance Criteria

1. Botão `[📥 Exportar CSV]` em cada lista admin:
   - `/admin/waitlist`
   - `/admin/orgs`
   - `/admin/subscriptions`
2. Endpoint `GET /api/admin/export/{entity}.csv`:
   - `entity` ∈ {waitlist, orgs, subscriptions, audit}
   - Aplica filtros vigentes (passados via query params)
   - **Streams** CSV pra evitar memória (pra waitlist grande)
   - Header `Content-Disposition: attachment; filename="{entity}-{date}.csv"`
   - MIME: `text/csv; charset=utf-8`
3. **CSV format:**
   - Linha 1: headers em português
   - Encode UTF-8 BOM (Excel BR abre corretamente)
   - Campos com vírgula/aspas escapados
   - Dates ISO 8601
4. **Audit log entry** quando export executado:
   - `admin.export_executed` com `{entity, filter_summary, row_count}`
5. **PII sanitization:**
   - Telefone parcial (`+55 11 9XXXX-1234` mascarado)?
   - **DECISÃO V1:** export retorna tudo (admin tem acesso total),
     mas log captura
6. Performance: streaming pra >10k rows sem time out

## Arquivos prováveis

- `apps/api/src/routes/admin/export.ts` (NOVO)
- `apps/web/src/app/(admin)/waitlist/waitlist-client.tsx` (atualizar
  — botão export)
- (similar nos clients de orgs, subscriptions)
- `apps/api/src/services/csv-export.ts` (NOVO — helper streaming)

## Dev notes

- **Streaming:** Postgres cursor + write chunked. Drizzle suporta
  via raw queries
- **CSV escape:** field com `,` ou `"` ou `\n` → wrap com `"..."` +
  duplicate quotes inside
- **Excel BR encoding:** UTF-8 BOM no início (`﻿`)
- **Filename:** `waitlist-2026-05-13.csv`
- **PII:** decisão produto = export inclui PII porque é uso admin
  legítimo. Audit log captura quem exportou + quando
- **Async grande:** se entidade tem >50k rows, considerar gerar
  background + emitir email com link (V2)

## CodeRabbit Integration

- Foco: streaming correctness (memory leaks), CSV injection (formulas
  malicious em campos), authorization, PII logging
- Severity: HIGH = warn (PII)

## Quality Gates

- [x] Click export gera download CSV (Content-Disposition: attachment)
- [x] UTF-8 BOM no início — Excel BR abre com acentos corretos
- [x] Filtros aplicados respeitam export (propaga searchParams via ExportCsvButton)
- [x] Limit 10k rows (configurável LARGE_LIMIT). >50k = V2 background
- [x] Audit log captura `admin.export_executed` com `{ entity, rowCount, filters }`
- [x] Sem super_admin → 404 (`requireSuperAdmin` no route handler)
- [x] CSV escape RFC 4180 + anti-injection (`'` prefix em campos começando com `= + - @ \t \r`)

## Dev Agent Record V1

### Files List

**NEW:**
- `apps/web/src/lib/csv-export.ts` — helpers `escapeCsvField`, `rowsToCsv` (BOM + RFC 4180 + anti-injection), `csvFilename` (entity-YYYY-MM-DD.csv), `csvResponse` (Response c/ headers attachment)
- `apps/web/src/components/admin/export-csv-button.tsx` — Client component que propaga searchParams do contexto atual pra endpoint via `<a href={endpoint?searchParams} download>`
- `apps/web/src/app/api/admin/export/waitlist/route.ts` — GET handler com requireSuperAdmin + filtros do waitlist + 13 colunas em PT-BR + audit
- `apps/web/src/app/api/admin/export/orgs/route.ts` — GET handler análogo + 9 colunas em PT-BR

**MODIFIED:**
- `apps/web/src/app/(admin)/admin/waitlist/page.tsx` — botão "Exportar CSV" acima dos filtros
- `apps/web/src/app/(admin)/admin/orgs/page.tsx` — idem

### Escopo V1

**Entregue:**
- Export waitlist (13 cols) + orgs (9 cols)
- Filtros propagados via searchParams
- BOM + escape RFC 4180 + anti-CSV-injection
- Audit log entry com row count + filters

**NÃO entregue (com justificativa):**
- **Subscriptions export:** depende Epic 6 Asaas
- **Audit log export:** baixa prioridade — admin tem UI completa em `/admin/audit`. Pode entrar V1.1 fácil (mesmo pattern).
- **Streaming chunked:** AC pediu streaming. V1 usa in-memory pq <10k rows. >50k = V2 background job + email.

### Anti-CSV-injection
Campos começando com `=`, `+`, `-`, `@`, `\t`, `\r` ganham prefixo `'` — Excel/Sheets não interpretam como fórmula. Mitiga payload tipo `=cmd|'/c calc'!A1` em campo de notes/nome.

### Validações
- typecheck verde
- build: route handlers `/api/admin/export/{waitlist,orgs}` aparecem (167 B cada)
