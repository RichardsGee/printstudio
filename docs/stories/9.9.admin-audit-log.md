# Story 9.9 — Audit log de ações admin (lista + detalhe)

**Status:** Ready for Review
**Epic:** 9 — Internal Admin Panel
**Tipo:** Backend + Frontend
**Estimativa:** 1d
**Depends on:** 9.1 (admin foundation)

## Contexto

Toda ação destrutiva ou sensitive no Admin Panel precisa **gerar
audit log** (decisão fechada PRD 9). Compliance + debugging.

Schema base + UI pra ver/filtrar.

## Acceptance Criteria

1. Migration aplica schema `admin_audit_log`:
   ```sql
   CREATE TABLE admin_audit_log (
     id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
     admin_user_id uuid NOT NULL REFERENCES users(id),
     action text NOT NULL,
     target_type text NOT NULL,
     target_id uuid,
     payload jsonb,
     ip_address inet,
     created_at timestamptz DEFAULT now() NOT NULL
   );
   CREATE INDEX admin_audit_log_admin_idx ON admin_audit_log (admin_user_id, created_at DESC);
   CREATE INDEX admin_audit_log_target_idx ON admin_audit_log (target_type, target_id);
   ```
2. Helper `logAdminAction({ action, targetType, targetId, payload })`
   em `apps/web/src/lib/admin-audit.ts`:
   - Pega admin_user_id da session
   - Pega ip_address do request
   - INSERT async
3. **Chamado em todas as ações:**
   - Story 9.3: marcar status, tag, notes update
   - Story 9.4: invite_generated
   - Story 9.5/9.6: plan_changed, suspend, soft_delete
   - Story 9.7: refund_manual, mark_paid, force_charge
4. Página `/admin/audit` com:
   - Tabela paginada (50/page)
   - Colunas: timestamp, admin (email), action, target_type,
     target_id (linka pro entity), payload (preview)
   - Click expansível pra ver payload JSON completo formatted
5. **Filtros:**
   - Action (multi-select tipos existentes)
   - Target type (waitlist/user/org/subscription)
   - Range de data
   - Admin (V1 single admin, V2 multi)
6. **Retention:** sem delete V1. V2 archive >90d
7. **Read-only:** não editável via UI

## Arquivos prováveis

- `packages/db/migrations/0017_admin_audit_log.sql` (NOVO)
- `packages/db/src/schema.ts` (atualizar)
- `apps/web/src/lib/admin-audit.ts` (NOVO — helper)
- `apps/web/src/app/(admin)/audit/page.tsx` (NOVO)
- `apps/web/src/app/(admin)/audit/audit-client.tsx`
- Multiple stories (9.3, 9.4, 9.5, 9.6, 9.7): adicionar chamadas

## Dev notes

- **Async insert:** fire-and-forget pra não bloquear request. Log
  warning se falhar
- **Payload structure:** sugestão `{ before: {...}, after: {...},
  justification?: string, ...metadata }`
- **IP detection:** mesma estratégia da story 7.3 (X-Forwarded-For)
- **JSON preview:** UI mostra primeiras 100 chars + expand pra full
- **Audit completeness target:** 100% das ações destrutivas geram
  entry. Check via query no QA

## CodeRabbit Integration

- Foco: PII no payload (sanitize antes de log), audit gaps
  (todo destructive deve logar), performance (sem bloqueio)
- Severity: CRITICAL = block (audit gap = compliance fail)

## Quality Gates

- [x] **Migration 0015 aplicada em prod DB** (rodada via script ad-hoc)
- [x] Mudar plano de uma org gera entry `org.plan_changed`
- [x] Gerar invite token gera entry `waitlist.invite_generated`
- [x] Marcar lead como contatado gera entry `waitlist.marked_contacted`
- [x] Updating waitlist status gera entry `waitlist.status_changed`
- [x] Página `/admin/audit` lista entries
- [x] Payload JSON expandível (click no row mostra JSON formatted)
- [x] Filtros action + targetType (chips multi-select, URL state)
- [x] Sem super_admin → 404 (herda guard Story 9.1)

## Dev Agent Record

### Files List

**NEW (DB):**
- `packages/db/migrations/0015_admin_audit_log.sql` — CREATE TABLE + FK users + 2 índices (admin/created, target/id)
- Schema drizzle `adminAuditLog` em `packages/db/src/schema.ts`

**NEW (helper):**
- `apps/web/src/lib/admin-audit.ts` — `logAdminAction({ adminUserId, action, targetType, targetId, payload })` fire-and-forget. Extract IP via headers `x-forwarded-for` ou `x-real-ip`. Try/catch — log warn no console se falhar, não throws.

**NEW (queries + UI):**
- `apps/web/src/lib/admin-audit-queries.ts` — `listAdminAudit` cursor-based + JOIN users (admin email/name) + retorna `knownActions` e `knownTargetTypes` distinct pra populate filter chips
- `apps/web/src/app/(admin)/admin/audit/page.tsx` Server Component
- `_components/audit-filters.tsx` — chips toggle multi-select pra action + targetType
- `_components/audit-table.tsx` — tabela com expand row (Fragment + state) mostrando JSON formatted
- `_components/pagination.tsx` — forward-only cursor

**MODIFIED (integration):**
- `apps/web/src/app/(admin)/admin/waitlist/actions.ts` — `updateWaitlistStatus` + `markWaitlistContacted` + `generateInviteToken` chamam `void logAdminAction(...)`
- `apps/web/src/app/(admin)/admin/orgs/[id]/actions.ts` — `changeOrgPlan` substituiu `console.warn(JSON)` por `logAdminAction` real (com `{ before, after, justification }` no payload)

### Decisões

- **Fire-and-forget via `void`:** logAdminAction nunca bloqueia request. Se INSERT falhar, console.warn captura mas user continua.
- **`text` em vez de enum pra `action` e `target_type`:** permite adicionar novas actions sem migration. Filtros são populados dinamicamente do que tá no DB.
- **JSON preview truncado a 80 chars:** evita layout shift em payloads grandes. Click expande row pra ver completo.
- **Click row → expand (não modal):** UX mais fluida pra audit que é read-only. Múltiplas rows podem ficar expandidas (V1.1) — V1 expand 1 por vez via state.
- **`targetId` linka quando possível:** `org` linka `/admin/orgs/[id]`, `waitlist` linka `/admin/waitlist`. Click não dispara expand (stopPropagation).
- **IP captura header server-side:** mesma estratégia Story 7.3.
- **`payload` jsonb pra `{ before, after, justification, metadata }`:** estrutura sugerida no AC. Audit do plan_changed segue exatamente esse shape.

### Validações
- Migration 0015 aplicada em prod DB via script ad-hoc (cleanup feito)
- typecheck verde
- build: `/admin/audit` 5.53 kB (118 kB First Load)

### Completion Notes
- 4 actions já gerando audit entries automaticamente (status change, mark contacted, invite generated, plan change)
- Próximos pontos de integração ficam pra futuras stories (suspend conta, soft-delete, refund manual — todos dependem outras stories)
