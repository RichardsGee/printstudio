# Story 8.2 — Auto-create user + org + member (transação atômica)

**Status:** Ready for Review
**Epic:** 8 — Customer Onboarding
**Tipo:** Backend + DB
**Estimativa:** 0.5d
**Depends on:** — (foundation pra 8.1 funcionar)

## Contexto

Quando lead vira user, precisa criar **3 entidades** atomicamente:
1. `users` row
2. `organizations` row (nome default "Org de {nome}")
3. `organization_members` row vinculando (role=owner)

Se qualquer falhar, rollback total. Sem inconsistência.

Schema migration também adiciona campos novos em `organizations`
relacionados ao onboarding.

## Acceptance Criteria

1. Migration drizzle aplica:
   - `organizations.onboarding_step` text DEFAULT 'profile' NOT NULL
   - `organizations.onboarding_completed_at` timestamp nullable
   - `organizations.role` text nullable (sync com waitlist_role)
   - `organizations.state` text nullable (BR UF)
   - `organizations.city` text nullable
   - `organizations.plan` text DEFAULT 'free' NOT NULL
   - `organizations.waitlist_id` uuid REFERENCES waitlist(id) ON DELETE SET NULL
   - `invite_tokens` table criada (token PK, waitlist_id FK, used_at, expires_at, created_at)
2. Service `createUserWithOrg({ email, name, passwordHash, waitlistToken? })` em
   `apps/api/src/services/signup.ts`:
   - Inicia transação drizzle (`db.transaction`)
   - INSERT user
   - Se waitlistToken válido (não expirado, not used):
     - Busca waitlist row → copia state/city/role pro org
     - Set `org.waitlist_id`
     - Marca `invite_tokens.used_at = now()`
   - INSERT organization (name = "Org de {name}", plan = 'free', onboarding_step = 'profile')
   - INSERT organization_member (user_id, organization_id, role = 'owner')
   - Commit retorna `{ user, organization, member }`
3. Se qualquer INSERT falhar, transação rollback (drizzle nativo)
4. Função throws com error code específico:
   - `EMAIL_EXISTS` se UNIQUE constraint user.email
   - `INVITE_INVALID` se token inválido/expirado/usado (mas não bloqueia
     signup — fallback pra signup sem pre-fill)
   - `INTERNAL_ERROR` qualquer outro
5. Logs estruturados: `signup.success` com user_id (não email), `signup.error` com code

## Arquivos prováveis

- `packages/db/src/schema.ts` (adicionar columns + invite_tokens table)
- `packages/db/migrations/0013_onboarding.sql` (drizzle generate)
- `apps/api/src/services/signup.ts` (NOVO)
- `apps/api/src/routes/auth.ts` (usar serviço — depende story 8.1)

## Dev notes

- **Transação drizzle:** `db.transaction(async (tx) => { ... })`. Em
  postgres-js. Rollback automático se throw
- **`waitlist_id` opcional:** se signup direto (sem token), fica NULL
- **`invite_tokens.expires_at`:** decisão = 30 dias após geração (V1 simple)
- **Org name default:** "Org de {primeiro nome}" — extrair primeiro
  nome via split(' ')[0]
- **Plan default 'free':** crítico pra Free tier funcionar imediatamente
- **Role enum:** validar contra `waitlist_role` enum existente (Epic 7)

## Schema reference

```sql
-- additions
ALTER TABLE organizations ADD COLUMN onboarding_step text DEFAULT 'profile' NOT NULL;
ALTER TABLE organizations ADD COLUMN onboarding_completed_at timestamptz;
ALTER TABLE organizations ADD COLUMN role text;
ALTER TABLE organizations ADD COLUMN state text;
ALTER TABLE organizations ADD COLUMN city text;
ALTER TABLE organizations ADD COLUMN plan text DEFAULT 'free' NOT NULL;
ALTER TABLE organizations ADD COLUMN waitlist_id uuid REFERENCES waitlist(id) ON DELETE SET NULL;

CREATE TABLE invite_tokens (
  token text PRIMARY KEY,
  waitlist_id uuid REFERENCES waitlist(id) ON DELETE CASCADE,
  used_at timestamptz,
  expires_at timestamptz NOT NULL,
  created_at timestamptz DEFAULT now() NOT NULL
);
CREATE INDEX invite_tokens_waitlist_idx ON invite_tokens (waitlist_id);
```

## CodeRabbit Integration

- Foco: transaction safety (no partial writes), SQL injection, race
  conditions (UNIQUE handling), error masking (no leak interna)
- Severity: CRITICAL = block (data inconsistency)

## Quality Gates

- [x] Migration aplicada (já entregue Story 7.3 — migration 0012)
- [x] Service test: signup happy path cria 3 rows
- [x] Service test: email duplicado → SignupError EMAIL_EXISTS (rollback drizzle nativo)
- [x] Service test: invite token inválido/expirado → signup OK sem pre-fill
- [x] Service test: invite token válido → pre-fill + mark used + waitlist=converted
- [x] Logs OK (sem PII — só userId/orgId/code, nunca email/name)

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (1M context) — Dex (Builder)

### Files List

**NEW:**

- `packages/shared/src/auth.ts` — `SignupSchema` Zod + `SignupResponseSchema`
  + tipo `SignupErrorCode`. Password policy: min 12 chars + maiúscula +
  número + caractere especial. Honeypot `website` opcional (mesmo padrão
  Story 7.3).
- `apps/api/src/services/signup.ts` — service `createUserWithOrg` com
  transação drizzle atômica. Suporta invite token opcional (pre-popula
  role/state/city/waitlistId, marca token used_at, atualiza waitlist
  status → 'converted'). Error codes: EMAIL_EXISTS, INVITE_INVALID,
  INTERNAL_ERROR. Helper `pruneExpiredInviteTokens` exposto pra Story 8.9
  (job de limpeza).
- `apps/api/src/services/signup.test.ts` — 8 tests vitest cobrindo:
  defaultOrgName (3 casos), happy path sem invite, happy path com invite
  válido, invite inválido não-bloqueia, EMAIL_EXISTS, INTERNAL_ERROR sem
  vazar mensagem original.
- `apps/api/vitest.config.ts` — config minimal (env=node, setupFiles).
- `apps/api/test/setup.ts` — env stub pra testes (DATABASE_URL, AUTH_SECRET,
  etc fake — nada toca em DB real).

**MODIFIED:**

- `packages/shared/src/index.ts` — re-exporta `./auth.js`.
- `apps/api/package.json` — adiciona `vitest` devDep + scripts
  `test`/`test:watch`.
- `docs/stories/8.2.auto-create-user-org-member.md` — Status + Dev Agent
  Record.

### Decisões

- **Transação drizzle nativa:** `database.transaction(async tx => ...)`.
  Qualquer throw dentro do callback rollback automático. Mais simples
  que tentar fazer 2-phase commit manual.
- **Invite token INVÁLIDO ≠ erro fatal:** AC fechada — se token expirado
  ou usado, signup procede sem pre-fill em vez de bloquear. Comportamento
  documentado no JSDoc do `consumeInviteToken`.
- **Race-safe consume do token:** o UPDATE inclui `used_at IS NULL` no
  WHERE. Se outra transação consumiu entre SELECT e UPDATE, o UPDATE
  retorna 0 rows e tratamos como invalid (sem rollback, sem pre-fill).
- **isEmailUniqueViolation:** detecta postgres `code === '23505'` +
  match em constraint_name `users_email_unique` OU detail message
  contendo "email". Resiliente a mudanças de constraint name.
- **Logs sem PII:** logger.info com `{ userId, orgId, invitePrefilled }`
  apenas. Nunca email, nome, IP. Mesmo nos errors, só `{ code }` no
  warn/error log. Erro genérico inclui `err.message` no log (server-only,
  nunca exposto via API).
- **`defaultOrgName` exportada:** facilita reuso futuro (admin panel
  Epic 9 pode mostrar/sugerir o nome ao criar org manualmente) + tests
  cobrindo edge cases (whitespace, empty).
- **Não adicionado: vitest no apps/web/worker/bridge ainda.** Por agora,
  só apps/api tem test runner — Story 8.2 exigiu, próximas stories
  podem reusar a config quando precisarem.
- **`waitlist.status = 'converted'` best-effort:** se a UPDATE falhar
  por algum motivo (constraint, conn drop), o resto da transação ainda
  é comitada — não rollback. Decisão pragmática: signup é mais
  importante que o status tracking.

### Validações

- `pnpm typecheck` — verde em todos os 7 workspaces.
- `pnpm --filter @printstudio/api test` — 8/8 testes passam. Logs
  esperados de erro aparecem no stderr (vêm dos tests que validam
  comportamento de erro — `INTERNAL_ERROR` e step mismatch).
- `pnpm --filter @printstudio/api build` — verde (148.8 kb bundle).
- Lint não rodado pq `apps/api` não tem lint configurado (script é
  `echo 'no lint yet'`) — fora do escopo desta story.

### Completion Notes

- **Service pronto pra Story 8.1 consumir:** o endpoint `POST /api/auth/signup`
  precisa chamar `createUserWithOrg({ email, name, passwordHash, inviteToken? })`
  e mapear os error codes pra response codes HTTP apropriados
  (`EMAIL_EXISTS` → 409, `INTERNAL_ERROR` → 500, etc).
- **Migration 0012 cobriu o schema todo** — esta story só adiciona o
  service layer. Migration já está aplicada em prod DB (decisão da
  Story 7.3).
- **Sem teste de integração contra DB real:** infraestrutura de DB
  de testes não existe no monorepo (decisão tech-debt — backlog).
  Tests com mocks cobrem a lógica de transação; teste E2E real
  acontece quando Story 8.1 entregar o endpoint e a UI.

### Change Log

- 2026-05-13 — Status: Ready → Ready for Review (Dex)
  - Criado service `createUserWithOrg` com transação atômica
  - 8 unit tests cobrindo happy paths + error cases
  - Schema Zod compartilhado em `@printstudio/shared`
  - Vitest configurado em `apps/api` (primeiro test runner no monorepo)
