# Story 9.1 — Schema `is_super_admin` flag + middleware de acesso

**Status:** Ready for Review
**Epic:** 9 — Internal Admin Panel
**Tipo:** Backend + DB
**Estimativa:** 1d
**Depends on:** — (foundation)

## Contexto

Foundation do Admin Panel. Adicionar **flag boolean** `is_super_admin`
em `users` + middleware server-side que protege todas as rotas
`/admin/*`. Decisão fechada PRD 9: **single super_admin V1** (só
Richard).

Segurança crítica: flag **nunca** pode ser editável via API normal.

## Acceptance Criteria

1. Migration aplica:
   ```sql
   ALTER TABLE users ADD COLUMN is_super_admin boolean DEFAULT false NOT NULL;
   CREATE INDEX users_super_admin_idx ON users (is_super_admin) WHERE is_super_admin = true;
   ```
2. Middleware Next.js (`apps/web/src/middleware.ts` ou layout server
   component) em `(admin)/layout.tsx`:
   - Lê `session` via NextAuth
   - Busca `user.is_super_admin` no DB
   - Se NÃO super_admin → `notFound()` (404, não 403 — não revela
     existência da rota)
   - Se OK, passa pra page
3. `PATCH /api/users/:id` (endpoint existente ou novo) **REJEITA**
   qualquer payload contendo `is_super_admin`:
   - Server-side strip do campo
   - Logs warning "attempt to escalate privilege"
4. Helper `requireSuperAdmin()` em `apps/web/src/lib/auth.ts`:
   - Verifica session + flag
   - Lança erro com redirect se não passar
   - Usado em todas rotas `/admin/*`
5. Seeder ou SQL manual pra promover Richard:
   - `UPDATE users SET is_super_admin = true WHERE email = '{richard-email}';`
   - Documentar em `docs/architecture/admin-setup.md`

## Arquivos prováveis

- `packages/db/migrations/0015_super_admin.sql` (NOVO)
- `packages/db/src/schema.ts` (atualizar)
- `apps/web/src/lib/auth.ts` (estender — `requireSuperAdmin`)
- `apps/web/src/app/(admin)/layout.tsx` (NOVO — route group)
- `apps/web/src/middleware.ts` (atualizar ou criar — adicionar /admin/* protection)
- `apps/api/src/routes/users.ts` (validar PATCH não permite escalation)
- `docs/architecture/admin-setup.md` (NOVO — instructions)

## Dev notes

- **Single source of truth:** `users.is_super_admin` é a flag. Sem RBAC
  granular V1
- **404 vs 403:** retornar 404 esconde existência da rota (security
  through obscurity reforço, não primário)
- **Escalation prevention:** validar Zod schema input PATCH user — strip
  `is_super_admin` antes de pegar resto. Test crítico
- **Session check:** middleware verifica em CADA request /admin/*
  (não confia em cookie sozinho)
- **Route group `(admin)`:** isolated layout pra não vazar dependências
  pro `(app)` normal

## CodeRabbit Integration

- Foco: **privilege escalation** (CRÍTICO — payload validation, input
  sanitization), middleware bypass, session fixation
- Severity: CRITICAL = block (escalation gap)

## Quality Gates

- [ ] **Migration 0014 aplicada em prod DB** — pendência Richard (sandbox bloqueia ops destrutivas em prod DB compartilhado)
- [x] User normal acessa `/admin/*` → 404 (`requireSuperAdmin()` chama `notFound()`)
- [x] Super admin acessa `/admin/*` → 200 (layout renderiza após guard)
- [x] PATCH user com `is_super_admin: true` → 400 INVALID_PAYLOAD + warning log "users.escalation_attempt"
- [x] Sem flag, `requireSuperAdmin()` retorna 404 (via notFound — não throw)
- [ ] **Richard promoted via SQL** — pendência manual: `UPDATE users SET is_super_admin = true WHERE email = '<email>';`
- [x] Performance: 1 query ao DB (SELECT em users by id, index PK) — sub-millisegundo. Sem cache deliberado pra revoga imediata.

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (1M context) — Dex (Builder)

### Files List

**NEW (DB):**

- `packages/db/migrations/0014_super_admin.sql` — `ALTER TABLE users
  ADD COLUMN is_super_admin boolean DEFAULT false NOT NULL` + index
  parcial `users_super_admin_idx WHERE is_super_admin = true`.
- `packages/db/migrations/meta/_journal.json` — entry idx=14 adicionada.

**NEW (auth lib):**

- `apps/web/src/lib/admin-auth.ts` — `requireSuperAdmin()` server-only
  (lookup `users.is_super_admin`, `notFound()` se falhar) +
  `isSuperAdmin()` versão non-throwing pra checks UI.

**NEW (admin route group):**

- `apps/web/src/app/(admin)/layout.tsx` — layout próprio (sem
  AppHeader), header minimal com badge SUPER-ADMIN, validação via
  `requireSuperAdmin()`. Link "← App" pra voltar pro dashboard normal.
- `apps/web/src/app/(admin)/admin/page.tsx` — placeholder home com
  6 SectionCards apontando pras stories 9.2-9.9 (todos com badge
  EM-BREVE).

**NEW (API + tests):**

- `apps/api/src/routes/users.ts` — `PATCH /api/users/:id` com
  defesa profunda: Zod `.strict()` + pre-check `FORBIDDEN_FIELDS`
  pra auditoria + ownership check + warn log de tentativas.
- `apps/api/src/routes/users.test.ts` — 10 testes da defesa
  (camelCase/snake_case/kebab-case/__proto__ bypass attempts).

**NEW (docs):**

- `docs/architecture/admin-setup.md` — instruções de promoção via
  SQL, cheat sheet, garantias contra escalation, evolução V1.1/V2.

**MODIFIED:**

- `packages/db/src/schema.ts` — `users.isSuperAdmin` column drizzle.
- `apps/api/src/server.ts` — registra `registerUserRoutes`.
- `docs/stories/9.1.super-admin-schema.md` — Status + Dev Agent Record.

### Decisões

- **Lookup ao DB a cada request (sem cache):** Decisão deliberada
  pra revoga imediata. Custo: 1 query indexada (PK) — sub-ms. Não
  vale o trade-off de TTL com staleness em revoga emergencial.
- **404 em vez de 403:** AC literal. `notFound()` do Next renderiza
  página 404 que esconde existência da rota.
- **Defesa em 2 camadas no PATCH user:**
  1. Pre-check: itera `Object.keys(body)` procurando palavras-chave
     forbidden (`isSuperAdmin`, `is_super_admin`, `role`, etc).
     Loga warn pra auditoria. NÃO bloqueia aqui — só audita.
  2. Zod `.strict()`: rejeita 400 qualquer campo extra. Defesa
     primária.
- **Pre-check via Set lookup:** `FORBIDDEN_FIELDS` é constante
  centralizada — fácil adicionar campos novos no futuro (ex:
  `2fa_secret` quando entrar 2FA).
- **Tests cobrem bypass attempts criativos:** camelCase, snake_case,
  kebab-case (`is-super-admin`), `superuser`, `admin`, payload com
  apenas forbidden, mix de válido+forbidden. Todos rejeitados.
- **Helper `isSuperAdmin()` non-throwing separado:** útil pro futuro
  AppHeader mostrar link "Admin" condicionalmente sem virar exception
  flow.
- **`(admin)` route group em vez de `/admin/` plain:** isolated
  layout — não vaza `AppHeader` (kiosk header) pro admin. Padrão
  Next consistente com `(app)` e `(kiosk)`.

### Validações

- `pnpm typecheck` — verde em 7 workspaces.
- `pnpm --filter @printstudio/api test` — **41/41** passam (8 signup
  + 23 plan-limits + 10 escalation).
- `pnpm --filter @printstudio/web build` — verde. `/admin` 1.08 kB
  (110 kB First Load).

### Completion Notes

- **Foundation pronta:** Stories 9.2-9.10 vão adicionar páginas
  embaixo de `(admin)/admin/` que herdam o guard automaticamente.
- **Pendências pré-launch:**
  - Aplicar migration 0014 no prod DB
  - Promover Richard via SQL: `UPDATE users SET is_super_admin =
    true WHERE email = 'richardsgee69@gmail.com';`
- **NÃO criei middleware Next** (apps/web/src/middleware.ts) — o
  layout do route group já protege todas as páginas embaixo.
  Middleware seria redundante e adicionaria overhead em rotas não-admin.
- **PATCH endpoint sem caller atual:** criado como foundation
  defensiva. Quando UI de settings/profile precisar, já está pronto
  e seguro.

### Change Log

- 2026-05-13 — Status: Ready → Ready for Review (Dex)
  - Migration 0014 + schema drizzle
  - requireSuperAdmin guard com notFound()
  - PATCH /api/users/:id com defesa profunda (Zod strict + audit)
  - 10 tests cobrindo escalation attempts
  - docs/architecture/admin-setup.md
  - Placeholder /admin com map das stories vindouras
