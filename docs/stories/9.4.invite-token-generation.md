# Story 9.4 — Gerar token de convite (libera lead pra signup)

**Status:** Ready for Review
**Epic:** 9 — Internal Admin Panel
**Tipo:** Backend + Frontend
**Estimativa:** 0.5d
**Depends on:** 9.3 (admin waitlist UI), 8.2 (invite_tokens table)

## Contexto

Loop crítico: lead na waitlist → Richard clica "Liberar acesso" →
sistema gera token → Richard copia URL e envia manual (WhatsApp/email)
→ lead acessa /signup?invite={token} → conta criada com pre-fill.

## Acceptance Criteria

1. Botão `[🔗 Gerar link de convite]` em cada linha da waitlist
   (story 9.3) ou no detail drawer
2. Click → server action `generateInviteToken(waitlistId)`:
   - Gera token random URL-safe (`nanoid(32)` ou `ulid()`)
   - INSERT em `invite_tokens` (token, waitlist_id, expires_at = now() + 30 days)
   - Atualiza `waitlist.status = 'invited'`
   - Retorna URL completa: `https://app.guiaprint3d.com/signup?invite={token}`
3. UI mostra modal/toast com:
   - URL completa
   - Botão `[📋 Copiar pro clipboard]`
   - "Válido por 30 dias"
4. Audit log entry: `admin.invite_generated` com waitlist_id no payload
5. Re-gerar token: se waitlist já tem token ativo, invalida o antigo
   (`used_at = now()` ou similar marker) e cria novo
6. Listagem dos tokens ativos no detail da waitlist (opcional V1):
   - Histórico de tokens gerados + status (used/expired/active)

## Arquivos prováveis

- `apps/web/src/app/(admin)/waitlist/actions.ts` (atualizar story 9.3 —
  adicionar `generateInviteToken`)
- `apps/web/src/components/admin/invite-link-modal.tsx` (NOVO)

## Dev notes

- **Token format:** `nanoid(32)` produz strings URL-safe sem ambiguity
  characters (0/O, 1/l). 32 chars = ~190 bits entropia
- **30 days expiry:** decisão V1. Pode ser configurável V2
- **Atomic regenerate:** transação garante: invalida antigo + cria novo
  em 1 step
- **URL pattern:** `app.guiaprint3d.com/signup?invite={token}` —
  preserva domain pattern decidido no Epic 7
- **Copy to clipboard:** `navigator.clipboard.writeText()` com fallback
  manual select
- **Histórico V1.1:** mostrar tokens gerados anteriores. V1 só novo.

## CodeRabbit Integration

- Foco: token entropy (não usar Math.random), atomic regenerate (race
  condition), audit completeness
- Severity: CRITICAL = block (low entropy token)

## Quality Gates

- [x] Click no drawer detail gera token + URL aparece inline
- [x] Token funciona em `/signup?invite={token}` (Stories 8.1 + 8.9 já consomem)
- [x] Re-gerar invalida anteriores (UPDATE `used_at = now()` em todos ativos antes do INSERT novo, dentro de transação)
- [ ] **Audit log** pendente — Story 9.9 ainda não tem a tabela
- [x] Token expira em 30d (`expires_at = now + 30 days`)
- [x] Copy to clipboard via `navigator.clipboard.writeText()` + fallback `window.prompt`
- [x] `waitlist.status` muda pra 'invited' (preserva 'converted' se já estiver lá — não regride)

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (1M context) — Dex (Builder)

### Files List

**MODIFIED:**

- `apps/web/src/app/(admin)/admin/waitlist/actions.ts` — adicionada
  `generateInviteToken` server action + helpers `makeInviteToken`
  (crypto.randomBytes 24 bytes = base64url 32 chars, ~190 bits
  entropia) e `buildInviteUrl` (lê `NEXT_PUBLIC_APP_URL` ou
  `NEXT_PUBLIC_SITE_URL` ou fallback hardcoded `app.guiaprint3d.com`).
- `apps/web/src/app/(admin)/admin/waitlist/_components/waitlist-detail-drawer.tsx`
  — adicionado bloco "Convite de signup" com:
  - Botão "Gerar link de convite" (estado vazio)
  - Bloco mostrando URL gerado + botão "Copiar" / "Copiado" com
    feedback temporário + data de expiração + botão "Gerar novo
    (invalida este)" pra regenerar
- `docs/stories/9.4.invite-token-generation.md` — Status + Dev Agent Record.

### Decisões

- **`crypto.randomBytes` server-side (CSPRNG):** decisão CRITICAL
  do CodeRabbit. NUNCA `Math.random()` pra security tokens. 24 bytes
  base64url = 32 chars URL-safe (sem `=` padding, sem `/` ou `+`).
- **Transação atômica pra regenerar:** UPDATE `used_at = now()` em
  tokens ativos do mesmo waitlist_id + INSERT novo dentro de `db.transaction`.
  Garantia: nunca 2 tokens ativos simultâneos pro mesmo lead.
- **Status workflow preserva 'converted':** `CASE WHEN status IN
  ('converted') THEN status ELSE 'invited'`. Se lead já virou user
  (impossível pq não teria invite, mas defensive), regen do invite
  não regride o status.
- **URL completa pré-pronta:** retorna do server pra UI já com
  origin correto. Em dev local sem `NEXT_PUBLIC_APP_URL`, retorna
  o hardcoded `https://app.guiaprint3d.com` — Richard sabe que
  precisa trocar quando estiver testando localmente, mas a UX de
  copiar+colar via WhatsApp continua certa pro caso prod.
- **Sem audit log V1:** Story 9.9 vai criar a tabela. Por agora a
  ação fica registrada implicitamente via:
  - `invite_tokens` row com `created_at` timestamp
  - `waitlist.updated_at` timestamp
  - `waitlist.status` mudou pra 'invited'
- **`copied` state com timeout 2s:** feedback visual sem alert/toast.
  Padrão familiar (GitHub copy code).
- **Fallback `window.prompt`:** dev local sem HTTPS pode bloquear
  clipboard API. Prompt deixa user copiar manual com Ctrl+C.
- **Botão "Gerar novo" inline em vez de modal de confirmação:**
  decisão pragmática — admin sabe o que tá fazendo, modal seria
  fricção. Texto deixa claro "invalida este".

### Validações

- `pnpm --filter @printstudio/web typecheck` — verde.
- `pnpm --filter @printstudio/web build` — verde:
  - `/admin/waitlist` 8.42 kB (cresceu 0.75 kB com UI invite)

### Completion Notes

- **Loop completo end-to-end funcional:**
  1. Lead se cadastra na waitlist em `guiaprint3d.com` (Stories 7.2/7.3)
  2. Você abre `/admin/waitlist`, qualifica via filtros/tags
  3. Clica "Detalhes" do lead promissor
  4. Clica "Gerar link de convite" → recebe URL
  5. Clica "Copiar" → cola no WhatsApp do lead
  6. Lead acessa `/signup?invite=TOKEN` → form com pre-fill (Story 8.1)
  7. Submete signup → service consume token + linka org→waitlist
     (Stories 8.2 + 8.9)
  8. Lead vira user, vai pro onboarding wizard (Stories 8.3-8.5)
  9. `waitlist.status = 'converted'` no Admin Panel
- **Próximo Story 9.8** vai dashboard com funil drop-off consumindo
  `onboarding_events` (Story 8.10) — vê quantos viraram user dos
  invites mandados.

### Change Log

- 2026-05-13 — Status: Ready → Ready for Review (Dex)
  - generateInviteToken server action com crypto.randomBytes + transação
  - UI no drawer detail: botão gerar + bloco URL + copy
  - Loop waitlist→invite→signup→onboarding end-to-end funcional
