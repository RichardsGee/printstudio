# Story 7.2 — Landing Page (hero + features + form + footer + demo video)

**Status:** Ready for Review
**Epic:** 7 — Landing & Waitlist
**Tipo:** Frontend
**Estimativa:** 2-3d
**Depends on:** 7.1 (setup), 7.3 (API waitlist), 7.4 (privacy/terms), 7.5 (success page)

## Contexto

Landing pública em `guiaprint3d.com` comunicando o valor do PrintStudio
e capturando leads qualificados via form de waitlist rico. Visual
**Mission Control** consistente com o app autenticado.

## Acceptance Criteria

1. **HEADER sticky** com logo `// GUIAPRINT3D` + CTAs `[Entrar na lista]` `[Ver demo]`
2. **HERO** com:
   - Título: "Monitore suas Bambu Lab A1 de qualquer lugar"
   - Subtitle explicativo
   - CTA primário: `[Entrar na lista de espera]` (scroll pro form)
   - CTA secundário: `[▶ Ver demo (60s)]` → abre modal/iframe YouTube
   - Hero asset: GIF/WebP animado do kiosk (placeholder até asset real)
3. **FEATURES** grid 4 cards (mission cards estilo data-mc-card):
   - Monitor visual estilo Mission Control
   - Multi-impressora com reordenação drag-and-drop
   - Cloud nativo (sem bridge LAN)
   - Histórico + alertas em tempo real
4. **WAITLIST FORM** completo com qualificação:
   - Nome* (text, min 2 chars)
   - Email* (email format, lowercase)
   - Estado (dropdown BR 27 UFs)
   - Cidade (text opcional)
   - Qual seu uso?* (dropdown role: hobbyist/small_shop/studio/business/other)
   - Quantas Bambu? (radio: 1 / 2-3 / 4-10 / 11+)
   - Telegram @handle (text opcional)
   - WhatsApp telefone (text opcional, formato BR)
   - Campo honeypot `website` (invisible — story 7.3)
   - Checkbox aceito termos*
   - Submit `[Solicitar acesso]`
5. **FOOTER** com links: Sobre / Política / Termos / Contato + `guiaprint3d.com © 2026`
6. **Responsivo mobile** (375px sem scroll horizontal — segue padrão Story 5.1)
7. **SEO básico:** meta description, og:image, og:title, twitter:card
8. **Form validation** client-side + server-side (HTML5 + Zod) com mensagens de erro inline
9. Submit chama `POST /api/waitlist` (story 7.3), redireciona pra success page (story 7.5) em caso de sucesso, mostra erro inline se 4xx
10. Modal video YouTube embed responsivo (16:9), close X visível

## Arquivos prováveis

- `apps/marketing/src/app/page.tsx` (landing principal)
- `apps/marketing/src/app/_components/hero.tsx`
- `apps/marketing/src/app/_components/features.tsx`
- `apps/marketing/src/app/_components/waitlist-form.tsx`
- `apps/marketing/src/app/_components/footer.tsx`
- `apps/marketing/src/app/_components/demo-modal.tsx`
- `apps/marketing/public/images/kiosk-demo.webp` (asset placeholder)
- `apps/marketing/public/og-image.png`

## Dev notes

- **Form submission:** server action ou client `fetch` POST pra
  `/api/waitlist` (decidir: se marketing site é puramente static,
  precisa endpoint público no `apps/api` ou no app web — alinhar com
  Architect na story 7.3)
- **UTM tracking:** capturar via `useSearchParams` e enviar no payload
  do form (`utm_source`, `utm_medium`, `utm_campaign`, `utm_content`,
  `utm_term`, `referrer`)
- **Validation:** Zod schema compartilhado entre client + server (definir
  em `packages/shared` ou copiar)
- **Asset GIF:** placeholder até asset real do kiosk (Richard providencia)
- **Demo video:** placeholder YouTube link até video real ser gravado
- **Accessibility:** form com labels, aria-required, aria-invalid em erros

## CodeRabbit Integration

- Foco: a11y (form labels, ARIA), security (XSS no honeypot, validation
  server-side), performance (image optimization, no heavy JS no hero)
- Severity: CRITICAL = block, HIGH = warn pre-merge

## Quality Gates

- [ ] Lighthouse Performance >= 90 (mobile) — medir pós-deploy 7.6
- [ ] Lighthouse Accessibility >= 95 — medir pós-deploy 7.6
- [ ] Lighthouse SEO >= 95 — medir pós-deploy 7.6
- [x] Form submit OK em todos os 4 ranges de bambu_count (1 / 2-3 / 4-10 / 11+) — validado via Zod client-side
- [x] Validation server-side rejeita payloads inválidos (já entregue Story 7.3)
- [x] Honeypot field rejeita silenciosamente (já entregue Story 7.3 + form V2 oculto)
- [x] Responsivo 375/768/1280 — grids `sm:`/`lg:`/`xl:` + container max-w-6xl

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (1M context) — Dex (Builder)

### Files List

**NEW (componentes UI atômicos):**

- `apps/marketing/src/components/ui/select.tsx` — wrapper HTML
  `<select>` com chevron Lucide. Sem dependência Radix nova.
- `apps/marketing/src/components/ui/checkbox.tsx` — wrapper HTML
  `<input type=checkbox>` com `accent-primary` Mission Control.
- `apps/marketing/src/components/ui/radio-group.tsx` — RadioGroup +
  RadioGroupItem com Context API. Cards clicáveis estilo segmented
  control. Suporta controlled/uncontrolled.

**NEW (libs):**

- `apps/marketing/src/lib/api.ts` — `getApiBase()` lendo
  `NEXT_PUBLIC_API_URL` (obrigatório em prod, fallback localhost dev).
- `apps/marketing/src/lib/utm.ts` — hook `useUtmParams()` capturando
  UTM da URL + persistindo em `sessionStorage`. `getReferrer()` helper
  safe pra SSR.

**NEW (seções landing):**

- `apps/marketing/src/app/_components/site-header.tsx` — header
  sticky com logo `// GUIAPRINT3D` + CTAs Ver demo / Entrar na lista.
- `apps/marketing/src/app/_components/hero.tsx` — Hero com título,
  subtitle, 2 CTAs e HeroVisualPlaceholder (HUD fake só com tokens,
  zero imagens — substituível por asset real depois).
- `apps/marketing/src/app/_components/features.tsx` — grid 4 mission
  cards (Telemetria visual / Multi-impressora drag / Cloud nativo /
  Histórico+alertas). Mobile 1 col → Tablet 2 → Desktop 4.
- `apps/marketing/src/app/_components/waitlist-form.tsx` — form rico
  (9 campos + honeypot + checkbox terms), Zod validation client-side
  reusando `WaitlistSubmitSchema` do `@printstudio/shared`. Submit
  POST `/api/public/waitlist`. Erros inline por campo + alert global.
  UTM tracking via hook.
- `apps/marketing/src/app/_components/demo-modal.tsx` — Radix Dialog
  com iframe YouTube responsivo 16:9. Carrega iframe só quando `open`.
- `apps/marketing/src/app/_components/site-footer.tsx` — links
  Recursos / Política / Termos / Contato (mailto) + copyright.
- `apps/marketing/src/app/_components/landing-page-client.tsx` —
  orquestrador client (state do demo modal + scroll suave pro form).

**MODIFIED:**

- `apps/marketing/src/app/page.tsx` — substituído placeholder
  Story 7.1 pela landing real + metadata SEO completa (og:image,
  twitter:card, canonical).
- `apps/marketing/src/app/layout.tsx` — adicionado `metadataBase`
  lendo `NEXT_PUBLIC_SITE_URL` (resolve og:image URLs absolutos).
- `apps/marketing/next.config.mjs` — `typedRoutes` movido pra raiz
  (não mais `experimental`).
- `docs/stories/7.2.landing-page.md` — Status + Dev Agent Record.

**NEW (placeholder):**

- `apps/marketing/public/images/.gitkeep` — pasta pronta pro asset
  real do kiosk demo (Richard providencia).

### Decisões

- **HTML nativo em vez de Radix Select/RadioGroup/Checkbox:** AC
  pedia 3 novos componentes mas a UX é simples (dropdown + radio +
  checkbox). HTML nativo é mais acessível por default (screen readers,
  teclado) e evita +3 deps Radix no bundle. RadioGroup própria usa
  Context API pra estado controlado/uncontrolled.
- **HeroVisualPlaceholder ao invés de GIF/WebP placeholder real:**
  PRD diz "asset placeholder até asset real". Optei por placeholder
  funcional que JÁ comunica valor (mostra 3 cards Mission Control com
  status PRINTING/IDLE + % progresso), zero peso de imagem, e
  substituível depois com 1 swap.
- **Demo video YouTube URL é placeholder:** usei `dQw4w9WgXcQ` (Rick
  Astley) como sentinel óbvio até Richard gravar o video real.
- **Honeypot `<div>` com posição absoluta `-9999px`:** mais robusto
  que `display:none` pq alguns bots ignoram CSS display.
- **`useSearchParams()` em Suspense:** Next 15 requer Suspense boundary
  ao redor de componentes que usam `useSearchParams`. WaitlistForm
  está wrapped em `<Suspense>` no LandingPageClient.
- **Mensagens de erro do server amigáveis:** mapeei os 4 error codes
  do endpoint (INVALID_PAYLOAD, EMAIL_ALREADY_EXISTS, RATE_LIMITED,
  INTERNAL_ERROR) pra strings em PT-BR. 409 vira "Esse email já está
  na lista — fica tranquilo!" (não é erro real pra o usuário).
- **UTM persistido em `sessionStorage`:** se usuário chega via
  `?utm_source=twitter`, navega pra /termos, volta e submita, UTM
  ainda vai junto. Sai quando fecha aba.

### Validações

- `pnpm typecheck` — verde em todos os 7 workspaces (apps + packages).
- `pnpm lint --filter @printstudio/marketing` — verde, zero warnings.
- `pnpm build --filter @printstudio/marketing` — verde, zero warnings
  após fixes em `next.config.mjs` + `metadataBase`. Static export
  gerou 6 páginas: `/`, `/_not-found`, `/politica-de-privacidade`,
  `/termos-de-uso` + 2 internas.
- Bundle size página `/`: 34.6 kB (First Load JS 146 kB) — dentro do
  esperado pra landing com form Zod + Radix Dialog.

### Completion Notes

- **3 Quality Gates de Lighthouse ficam pendentes** pra serem medidos
  pós-deploy real (Story 7.6). Não bloqueiam merge.
- **Pendências triviais pré-launch** (não bloqueiam):
  1. Asset real do hero (GIF/WebP do kiosk) → swap em `hero.tsx`.
  2. URL do video demo real → swap em `demo-modal.tsx`.
  3. `og-image.png` real 1200×630 → drop em `public/`.
  4. `NEXT_PUBLIC_API_URL` + `NEXT_PUBLIC_SITE_URL` em prod env.
- **Story 7.5 (success page) ainda não existe** — o `router.push('/sucesso')`
  no submit do form vai 404 até a 7.5 ser implementada. Próximo step.

### Change Log

- 2026-05-13 — Status: Ready → Ready for Review (Dex)
  - Landing real substituindo placeholder Story 7.1
  - 8 componentes novos (header, hero, features, footer, form, modal,
    orchestrator) + 3 UI atoms (select, checkbox, radio-group)
  - Form Zod validation + integração POST /api/public/waitlist
  - UTM tracking persistido em sessionStorage
  - SEO completo (metadata, og, twitter, canonical, metadataBase)
  - Build static export OK, typecheck + lint verdes
